{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "ApplicationDefaults": {
    // MySql connection string
    // if the DB_CON_URL environment variable is defined, that is used instead
    "ConnectionString": "server=localhost;port=3306;database=replayserver;user=root;password=root"
  }
}
